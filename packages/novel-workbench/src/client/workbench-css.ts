/**
 * The novel-mode stylesheet, ported verbatim from
 * `docs/prototypes/2026-09-16-novel-mode/novel-mode-workbench.html`.
 *
 * Generated by `scripts/port-prototype-css.mjs`; edit the prototype first and
 * re-run the generator instead of hand-editing the string. The only changes are
 * the ones the generator lists: the prototype's own screen switcher is dropped,
 * the night selector binds to the frame (or the host's dark theme) instead of a
 * bare document, and the document reset is left to the host page.
 */
export const WORKBENCH_CSS = `/* ═══ tokens · 日间 / 夜间 ═══ */
:root {
  /* 底色四档：窗口底 → 主面板 → 卡片/阅读区 → 输入区/次级面板 */
  --bg-000: 0 0% 100%;
  --bg-100: 48 33.3% 97.1%;
  --bg-200: 53 28.6% 94.5%;
  --bg-300: 48 25% 92.2%;
  --border-100: 51 16.5% 84.5%;
  --border-200: 48 14% 74%;
  --text-000: 60 2.6% 7.6%;
  --text-100: 60 2.6% 7.6%;
  --text-200: 60 2.6% 7.6%;
  --accent-brand: 15 63.1% 59.6%;
  --accent-brand-deep: 15 54.2% 51.2%;
  --accent-text: 15 55% 38%;
  --on-accent: 20 14% 10%;
  --ok: 150 22% 32%;
  --err: 2 45% 38%;
  --info: 212 20% 42%;
  --warn: 36 62% 32%;
  --f-none: 45 6% 46%;
  --f-port: 212 20% 46%;
  --f-river: 345 20% 40%;
  --f-press: 158 18% 36%;
  --f-bureau: 32 42% 45%;
  --font-ui: "PingFang SC", "Microsoft YaHei", system-ui, -apple-system, "Segoe UI", sans-serif;
  --font-serif: "Source Han Serif SC", "Noto Serif SC", "Songti SC", "SimSun", serif;
  --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  --fs-12: 12px;
  --fs-13: 13px;
  --fs-14: 14px;
  --fs-16: 16px;
  --fs-20: 20px;
  --read-size: 17px;
  --read-lh: 1.85;
  --read-measure: 40em;
  --s1: 4px; --s2: 8px; --s3: 12px; --s4: 16px; --s5: 20px; --s6: 24px; --s7: 32px;
  --r-card: 10px; --r-ctl: 6px; --r-key: 4px;
  --sh-1: 0 1px 2px hsl(30 10% 10% / .04), 0 0 0 .5px hsl(var(--border-100));
  --sh-2: 0 2px 8px hsl(30 10% 10% / .07), 0 0 0 .5px hsl(var(--border-100));
  --sh-3: 0 14px 38px hsl(30 10% 10% / .18), 0 0 0 .5px hsl(var(--border-100));
  --t-fast: 150ms; --t-base: 220ms;
  --app-w: 1440px;
}
[data-novel-workbench="frame"][data-nw-theme="night"],
body[data-ds-dark-theme] [data-novel-workbench="frame"] {
  --bg-000: 60 2.1% 18.4%;
  --bg-100: 60 2.7% 14.5%;
  --bg-200: 30 3.3% 11.8%;
  --bg-300: 60 2.6% 7.6%;
  /* 实测线值在夜间低于可辨识阈值，按两档提升以保证 1px 细线可见（见 design-tokens.md） */
  --border-100: 30 4% 22%;
  --border-200: 30 5% 30%;
  --text-000: 48 33.3% 97.1%;
  --text-100: 48 33.3% 97.1%;
  --text-200: 48 33.3% 97.1%;
  --accent-brand: 15 63.1% 59.6%;
  --accent-brand-deep: 15 60% 53%;
  --accent-text: 15 63.1% 59.6%;
  --on-accent: 20 14% 10%;
  --ok: 150 18% 60%;
  --err: 5 45% 62%;
  --info: 212 16% 68%;
  --warn: 36 48% 62%;
  --f-none: 45 6% 62%;
  --f-port: 212 18% 64%;
  --f-river: 345 22% 66%;
  --f-press: 158 16% 60%;
  --f-bureau: 32 40% 62%;
  --sh-1: 0 1px 2px hsl(0 0% 0% / .3), 0 0 0 .5px hsl(var(--border-100));
  --sh-2: 0 2px 8px hsl(0 0% 0% / .35), 0 0 0 .5px hsl(var(--border-100));
  --sh-3: 0 14px 38px hsl(0 0% 0% / .5), 0 0 0 .5px hsl(var(--border-100));
}

*, *::before, *::after { box-sizing: border-box; }
[data-novel-workbench="frame"] { margin: 0; }
body {
  background: hsl(var(--bg-300));
  color: hsl(var(--text-100) / .92);
  font-family: var(--font-ui);
  font-size: var(--fs-13);
  line-height: 1.55;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}
button { font: inherit; color: inherit; cursor: pointer; background: none; border: 0; }
input, textarea, select { font: inherit; color: inherit; }
h1, h2, h3, h4 { margin: 0; font-family: var(--font-serif); font-weight: 600; letter-spacing: .01em; }
p { margin: 0; }
svg { display: block; }
:focus-visible { outline: 2px solid hsl(var(--accent-brand)); outline-offset: 2px; border-radius: var(--r-key); }
[hidden] { display: none !important; }
.num { font-family: var(--font-mono); font-variant-numeric: tabular-nums; }
.mono { font-family: var(--font-mono); }
.muted { color: hsl(var(--text-200) / .7); }
.full { width: 100%; }
@media (prefers-reduced-motion: reduce) { * { transition-duration: 1ms !important; animation-duration: 1ms !important; } }

/* ═══ 舞台与应用骨架 ═══ */
.stage { width: var(--app-w); height: 900px; }
.app {
  position: relative;
  width: 100%; height: 100%;
  display: grid;
  grid-template-columns: 248px minmax(0, 1fr) 296px;
  grid-template-rows: 44px minmax(0, 1fr) 88px;
  grid-template-areas: "top top top" "rail main side" "composer composer composer";
  background: hsl(var(--bg-000));
  border: 1px solid hsl(var(--border-100));
  border-radius: var(--r-card);
  overflow: clip;
  box-shadow: var(--sh-2);
}

/* 顶栏 */
.topbar {
  grid-area: top;
  display: flex; align-items: center; gap: var(--s3);
  padding: 0 var(--s4);
  background: hsl(var(--bg-100));
  border-bottom: 1px solid hsl(var(--border-100));
}
.topbar .brand { font-family: var(--font-serif); font-size: var(--fs-14); font-weight: 600; color: hsl(var(--text-000)); }
.topbar .sep { width: 1px; height: 18px; background: hsl(var(--border-100)); }
.topbar .ctx { font-size: var(--fs-13); color: hsl(var(--text-200) / .75); }
.topbar .right { margin-left: auto; display: flex; align-items: center; gap: var(--s2); }
.vbadge {
  font-family: var(--font-mono); font-size: var(--fs-12); font-weight: 600;
  padding: 2px 7px; border-radius: var(--r-key);
  background: hsl(var(--accent-brand) / .14); color: hsl(var(--accent-text));
  border: 1px solid hsl(var(--accent-brand) / .35);
}

/* 左栏 */
.rail {
  grid-area: rail;
  display: flex; flex-direction: column;
  background: hsl(var(--bg-100));
  border-right: 1px solid hsl(var(--border-100));
  min-height: 0;
}
.rail-scroll { flex: 1; overflow-y: auto; padding: var(--s3) var(--s2) var(--s2); }
.rail-foot { border-top: 1px solid hsl(var(--border-100)); padding: var(--s2); }
.grp + .grp { margin-top: var(--s5); }
.grp-head {
  display: flex; align-items: center; gap: 6px;
  padding: 0 6px var(--s2);
  font-size: var(--fs-12); color: hsl(var(--text-200) / .68);
}
.grp-head .count { margin-left: auto; font-family: var(--font-mono); }
.item {
  display: flex; align-items: center; gap: var(--s2);
  width: 100%; padding: 5px 8px;
  border-radius: var(--r-ctl);
  font-size: var(--fs-13); color: hsl(var(--text-100) / .88);
  text-align: left;
  transition: background var(--t-fast), color var(--t-fast);
}
.item:hover { background: hsl(var(--bg-000) / .8); }
.item[aria-current="true"], .item.on { background: hsl(var(--accent-brand) / .13); color: hsl(var(--accent-text)); font-weight: 600; }
.item .ico { flex: none; width: 15px; height: 15px; opacity: .8; }
.item .tail { margin-left: auto; font-family: var(--font-mono); font-size: var(--fs-12); color: hsl(var(--text-200) / .7); }
.item[aria-current="true"] .tail { color: hsl(var(--accent-text)); }
.vol-head {
  display: flex; align-items: center; gap: 6px;
  padding: var(--s3) 8px var(--s1);
  font-size: var(--fs-12); color: hsl(var(--text-200) / .7);
}
.vol-head .vol-meta { margin-left: auto; font-family: var(--font-mono); }
.ch-item { padding-left: 8px; gap: 7px; }
.ch-item .ord { flex: none; width: 18px; font-family: var(--font-mono); font-size: var(--fs-12); color: hsl(var(--text-200) / .7); text-align: right; }
.ch-item .t { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.st-dot { flex: none; width: 7px; height: 7px; border-radius: 50%; }
.st-dot.ok { background: hsl(var(--ok)); }
.st-dot.pend { background: hsl(var(--accent-brand)); }
.st-dot.plan { background: hsl(var(--border-200)); }
.st-dot.warn { background: hsl(var(--warn)); }

/* 右栏 */
.side {
  grid-area: side;
  display: flex; flex-direction: column;
  background: hsl(var(--bg-100));
  border-left: 1px solid hsl(var(--border-100));
  min-height: 0;
}
.side-scroll { flex: 1; overflow-y: auto; padding: var(--s3) var(--s3) var(--s4); }
.side-block + .side-block { margin-top: var(--s4); padding-top: var(--s4); border-top: 1px solid hsl(var(--border-100)); }
.side-title { display: flex; align-items: center; gap: 6px; font-size: var(--fs-12); color: hsl(var(--text-200) / .7); margin-bottom: var(--s2); }
.side-title .more { margin-left: auto; font-size: var(--fs-12); color: hsl(var(--accent-text)); }

/* 主画布 */
.main { grid-area: main; min-height: 0; display: flex; flex-direction: column; background: hsl(var(--bg-000)); }
.main-head {
  display: flex; align-items: flex-end; gap: var(--s3);
  padding: var(--s4) var(--s6) var(--s3);
  border-bottom: 1px solid hsl(var(--border-100));
}
.main-head .h-wrap { min-width: 0; }
.main-head h1 { font-size: var(--fs-20); color: hsl(var(--text-000)); }
.main-head .sub { margin-top: 3px; font-size: var(--fs-13); color: hsl(var(--text-200) / .72); }
.main-head .tools { margin-left: auto; display: flex; align-items: center; gap: var(--s2); flex: none; }
.main-body { flex: 1; overflow-y: auto; padding: var(--s5) var(--s6) var(--s6); min-height: 0; }
.main-body.flush { padding: 0; }

/* 底部输入条 */
.composer {
  grid-area: composer;
  display: grid; grid-template-columns: 176px minmax(0, 1fr) auto;
  gap: var(--s3); align-items: center;
  padding: var(--s3) var(--s4);
  background: hsl(var(--bg-300));
  border-top: 1px solid hsl(var(--border-100));
}
.composer .inline { display: flex; align-items: center; gap: var(--s2); }
#cmpThreadLabel { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* ═══ 通用控件（静置 / hover / active / disabled / loading 五态） ═══ */
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 7px;
  height: 32px; padding: 0 14px;
  border-radius: var(--r-ctl);
  border: 1px solid hsl(var(--border-100));
  background: hsl(var(--bg-000));
  color: hsl(var(--text-100) / .92);
  font-size: var(--fs-13); font-weight: 500;
  transition: background var(--t-fast), border-color var(--t-fast), color var(--t-fast), transform 60ms;
  white-space: nowrap;
}
.btn:hover { background: hsl(var(--bg-100)); border-color: hsl(var(--border-200)); }
.btn:active { transform: translateY(1px); background: hsl(var(--bg-200)); }
.btn.primary {
  background: hsl(var(--accent-brand)); border-color: hsl(var(--accent-brand)); color: hsl(var(--on-accent)); font-weight: 600;
}
.btn.primary:hover { background: hsl(var(--accent-brand) / .88); border-color: hsl(var(--accent-brand-deep)); }
.btn.primary:active { background: hsl(var(--accent-brand)); border-color: hsl(var(--accent-brand-deep)); color: hsl(var(--on-accent)); transform: translateY(1px); }
.btn.ghost { background: transparent; border-color: transparent; color: hsl(var(--text-200) / .8); }
.btn.ghost:hover { background: hsl(var(--bg-100)); color: hsl(var(--text-000)); }
.btn.danger { color: hsl(var(--err)); border-color: hsl(var(--err) / .4); background: hsl(var(--bg-000)); }
.btn.danger:hover { background: hsl(var(--err) / .1); border-color: hsl(var(--err) / .6); }
.btn[disabled], .btn.is-disabled {
  opacity: .45; cursor: not-allowed; background: hsl(var(--bg-200)); color: hsl(var(--text-200) / .8); border-color: hsl(var(--border-100));
}
.btn[disabled]:hover, .btn.is-disabled:hover { background: hsl(var(--bg-200)); border-color: hsl(var(--border-100)); transform: none; }
.btn.loading { cursor: progress; color: hsl(var(--text-200) / .85); }
.btn.primary.loading { background: hsl(var(--accent-brand) / .55); color: hsl(var(--on-accent)); }
.btn.sm { height: 26px; padding: 0 10px; font-size: var(--fs-12); }
.btn.lg { height: 36px; padding: 0 18px; font-size: var(--fs-14); }
.spinner {
  width: 12px; height: 12px; border-radius: 50%;
  border: 1.5px solid currentColor; border-right-color: transparent;
  animation: spin 700ms linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

.seg { display: inline-flex; padding: 1px; border-radius: var(--r-ctl); background: hsl(var(--bg-300)); border: 1px solid hsl(var(--border-100)); }
.seg button {
  height: 24px; padding: 0 10px; border-radius: 5px; font-size: var(--fs-12); color: hsl(var(--text-200) / .8);
  transition: background var(--t-fast), color var(--t-fast);
}
.seg button:hover { color: hsl(var(--text-000)); background: hsl(var(--bg-100)); }
.seg button[aria-pressed="true"] { background: hsl(var(--bg-000)); color: hsl(var(--text-000)); font-weight: 600; box-shadow: var(--sh-1); }
.seg button:disabled { opacity: .45; cursor: not-allowed; }

.chip {
  display: inline-flex; align-items: center; gap: 5px;
  height: 22px; padding: 0 8px; border-radius: var(--r-key);
  border: 1px solid hsl(var(--border-100));
  background: hsl(var(--bg-100));
  font-size: var(--fs-12); color: hsl(var(--text-200) / .82);
  white-space: nowrap;
}
.chip.ok { color: hsl(var(--ok)); border-color: hsl(var(--ok) / .35); background: hsl(var(--ok) / .08); }
.chip.warn { color: hsl(var(--warn)); border-color: hsl(var(--warn) / .4); background: hsl(var(--warn) / .1); }
.chip.err { color: hsl(var(--err)); border-color: hsl(var(--err) / .4); background: hsl(var(--err) / .08); }
.chip.info { color: hsl(var(--info)); border-color: hsl(var(--info) / .35); background: hsl(var(--info) / .08); }
.chip.accent { color: hsl(var(--accent-text)); border-color: hsl(var(--accent-brand) / .45); background: hsl(var(--accent-brand) / .12); }
.chip .dot { width: 6px; height: 6px; border-radius: 50%; background: currentColor; }
.tag { display: inline-flex; align-items: center; height: 22px; padding: 0 9px; border-radius: 999px; font-size: var(--fs-12); color: hsl(var(--text-200) / .8); border: 1px solid hsl(var(--border-100)); }

.field { display: flex; flex-direction: column; gap: 5px; }
.field label, .field .lbl { font-size: var(--fs-12); color: hsl(var(--text-200) / .75); }
.inp, .ta, .sel {
  width: 100%; height: 32px; padding: 0 10px;
  border: 1px solid hsl(var(--border-100)); border-radius: var(--r-ctl);
  background: hsl(var(--bg-000)); color: hsl(var(--text-100) / .92);
  font-size: var(--fs-13);
  transition: border-color var(--t-fast), background var(--t-fast);
}
.ta { height: auto; min-height: 62px; padding: 8px 10px; resize: vertical; line-height: 1.6; }
.inp:hover, .ta:hover, .sel:hover { border-color: hsl(var(--border-200)); }
.inp:focus, .ta:focus, .sel:focus { outline: none; border-color: hsl(var(--accent-brand)); box-shadow: 0 0 0 3px hsl(var(--accent-brand) / .16); }
.inp[disabled], .ta[disabled], .sel[disabled] { opacity: .5; cursor: not-allowed; }
.inp::placeholder, .ta::placeholder { color: hsl(var(--text-200) / .5); }

.switch { display: inline-flex; align-items: center; gap: 7px; font-size: var(--fs-12); color: hsl(var(--text-200) / .8); }
.switch .track { width: 30px; height: 17px; border-radius: 999px; background: hsl(var(--border-200)); position: relative; transition: background var(--t-fast); flex: none; }
.switch .track::after { content: ""; position: absolute; top: 2px; left: 2px; width: 13px; height: 13px; border-radius: 50%; background: hsl(var(--bg-000)); transition: transform var(--t-fast); box-shadow: var(--sh-1); }
.switch[aria-checked="true"] .track { background: hsl(var(--accent-brand)); }
.switch[aria-checked="true"] .track::after { transform: translateX(13px); }
.switch:hover .track { box-shadow: 0 0 0 3px hsl(var(--accent-brand) / .14); }

.card { background: hsl(var(--bg-200)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); padding: var(--s4); box-shadow: var(--sh-1); }
.card.plain { background: hsl(var(--bg-000)); }
.card.flat { background: transparent; box-shadow: none; }
.card-h { display: flex; align-items: center; gap: var(--s2); margin-bottom: var(--s3); }
.card-h .t { font-size: var(--fs-14); font-weight: 600; color: hsl(var(--text-000)); }
.card-h .r { margin-left: auto; display: flex; align-items: center; gap: 6px; }
.kv { display: grid; grid-template-columns: 64px 1fr; gap: 4px var(--s3); font-size: var(--fs-13); }
.kv dt { color: hsl(var(--text-200) / .7); }
.kv dd { margin: 0; color: hsl(var(--text-100) / .9); }
.rule { height: 1px; background: hsl(var(--border-100)); border: 0; margin: var(--s4) 0; }
.note { font-size: var(--fs-12); color: hsl(var(--text-200) / .7); }
.lead { font-size: var(--fs-14); color: hsl(var(--text-200) / .85); }
.grid { display: grid; gap: var(--s4); }
.g2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.g3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
.g4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }
.row { display: flex; align-items: center; gap: var(--s2); }
.row.between { justify-content: space-between; }
.col { display: flex; flex-direction: column; gap: var(--s2); }
.stack > * + * { margin-top: var(--s3); }
.progress { height: 4px; border-radius: 999px; background: hsl(var(--bg-300)); overflow: hidden; }
.progress > i { display: block; height: 100%; background: hsl(var(--accent-brand)); }
.progress.ok > i { background: hsl(var(--ok)); }

/* 骨架屏 */
.sk { border-radius: var(--r-key); background: linear-gradient(90deg, hsl(var(--bg-300)), hsl(var(--bg-200)), hsl(var(--bg-300))); background-size: 240px 100%; animation: shimmer 1.5s linear infinite; }
@keyframes shimmer { from { background-position: -120px 0; } to { background-position: 120px 0; } }
.sk-line { height: 10px; margin-bottom: 8px; }
.sk-card { height: 72px; border-radius: var(--r-card); margin-bottom: 10px; }

/* ═══ 故事地图 ═══ */
.map-wrap { position: relative; height: 100%; min-height: 0; display: flex; flex-direction: column; }
.map-bar {
  display: flex; align-items: center; gap: var(--s2);
  padding: var(--s3) var(--s6);
  border-bottom: 1px solid hsl(var(--border-100));
  background: hsl(var(--bg-000));
  flex-wrap: wrap;
}
.map-bar .grow { flex: 1; }
.search { position: relative; }
.search .inp { width: 188px; padding-left: 26px; }
.search svg { position: absolute; left: 8px; top: 9px; opacity: .55; }
.pop {
  position: absolute; z-index: 30; top: calc(100% + 4px); left: 0; width: 240px;
  background: hsl(var(--bg-000)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-ctl);
  box-shadow: var(--sh-3); padding: 4px; max-height: 240px; overflow-y: auto;
}
.pop button { display: flex; align-items: center; gap: 6px; width: 100%; padding: 6px 8px; border-radius: var(--r-key); font-size: var(--fs-13); text-align: left; }
.pop button:hover { background: hsl(var(--bg-100)); }
.pop .hint { padding: 6px 8px; font-size: var(--fs-12); color: hsl(var(--text-200) / .7); }
.map-vp { position: relative; flex: 1; min-height: 0; overflow: hidden; cursor: grab; background: hsl(var(--bg-000)); }
.map-vp.dragging { cursor: grabbing; }
.map-vp svg { width: 100%; height: 100%; }
.map-foot { display: flex; align-items: center; gap: var(--s3); padding: var(--s2) var(--s6) var(--s3); border-top: 1px solid hsl(var(--border-100)); flex-wrap: wrap; }
.legend { display: flex; align-items: center; gap: var(--s3); flex-wrap: wrap; font-size: var(--fs-12); color: hsl(var(--text-200) / .78); }
.legend .li { display: inline-flex; align-items: center; gap: 5px; }
.legend .sw { width: 9px; height: 9px; border-radius: 50%; }
.li { display: inline-flex; align-items: center; gap: 5px; }
.sw { width: 9px; height: 9px; border-radius: 50%; flex: none; }
.legend .ln { width: 18px; height: 0; border-top: 2px solid hsl(var(--border-200)); }
.legend .ln.dash { border-top-style: dashed; }
/* 图谱图元 */
.n-halo { fill: hsl(var(--accent-brand) / .12); }
.n-body { stroke: hsl(var(--bg-000)); stroke-width: 1.5; cursor: pointer; transition: opacity var(--t-base); }
.n-body.sel { stroke: hsl(var(--accent-brand)); stroke-width: 2.5; }
.n-hit { fill: transparent; cursor: pointer; }
.n-label { font-family: var(--font-ui); font-size: 11.5px; fill: hsl(var(--text-100) / .9); pointer-events: none; }
.n-label.sel { font-weight: 600; fill: hsl(var(--text-000)); }
.n-emo { stroke: hsl(var(--bg-000)); stroke-width: 1.5; pointer-events: none; }
.n-debt { pointer-events: none; }
.n-debt text { font-family: var(--font-mono); font-size: 8.5px; font-weight: 700; fill: hsl(var(--on-accent)); }
.n-pin { pointer-events: none; }
.n-g.dim { opacity: .26; }
.n-g.hidden { display: none; }
.e-line { fill: none; stroke: hsl(var(--border-200)); stroke-width: 1.4; transition: opacity var(--t-base), stroke var(--t-fast); }
.e-line.back { stroke-dasharray: 3 3; }
.e-line.hot { stroke: hsl(var(--accent-brand)); stroke-width: 2; }
.e-line.dim { opacity: .18; }
.e-hit { fill: none; stroke: transparent; stroke-width: 14px; cursor: pointer; }
.cluster { fill: hsl(var(--border-100) / .5); stroke: hsl(var(--border-100)); stroke-width: 1; }
.cluster-label { font-family: var(--font-mono); font-size: 10.5px; fill: hsl(var(--text-200) / .7); }
.bubble { fill: hsl(var(--bg-200)); stroke: hsl(var(--border-200)); stroke-width: 1; cursor: pointer; }
.bubble:hover { fill: hsl(var(--bg-300)); }
.bubble-text { font-family: var(--font-mono); font-size: 11px; fill: hsl(var(--text-100) / .85); pointer-events: none; }
.map-tip {
  position: absolute; z-index: 20; max-width: 280px; padding: 8px 10px;
  background: hsl(var(--bg-000)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-ctl);
  box-shadow: var(--sh-3); font-size: var(--fs-12); color: hsl(var(--text-100) / .9); pointer-events: none;
}
.map-tip b { font-weight: 600; color: hsl(var(--text-000)); }
.map-tip .d { display: block; margin-top: 4px; color: hsl(var(--text-200) / .8); }

/* ═══ 提案审阅 ═══ */
.rv-head { display: flex; align-items: center; gap: var(--s3); flex-wrap: wrap; font-size: var(--fs-13); color: hsl(var(--text-200) / .8); }
.rv-head .path { display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-mono); font-weight: 600; color: hsl(var(--text-000)); }
.rv-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(0, .95fr); gap: var(--s5); align-items: start; margin-top: var(--s4); }
.ms-scroll { max-height: 268px; overflow: hidden; position: relative; }
.ms-scroll::after { content: ""; position: absolute; left: 0; right: 0; bottom: 0; height: 64px; background: linear-gradient(hsl(var(--bg-200) / 0), hsl(var(--bg-200))); pointer-events: none; }
.ms-body { font-family: var(--font-serif); font-size: var(--read-size); line-height: var(--read-lh); max-width: var(--read-measure); color: hsl(var(--text-100) / .92); }
.ms-body p + p { margin-top: .82em; }
.ms-body p { text-indent: 0; }
.read-body { font-family: var(--font-serif); font-size: var(--read-size); line-height: var(--read-lh); max-width: var(--read-measure); color: hsl(var(--text-100) / .93); }
.read-body p + p { margin-top: .86em; }
.read-body mark { background: hsl(var(--accent-brand) / .2); color: inherit; border-radius: 2px; padding: 0 2px; box-shadow: inset 0 -1px 0 hsl(var(--accent-brand) / .5); }
.ms-end { font-family: var(--font-mono); color: hsl(var(--text-200) / .6); letter-spacing: .3em; }
.change-row { display: flex; align-items: center; gap: var(--s2); padding: 9px 10px; border-radius: var(--r-ctl); background: hsl(var(--bg-200)); border: 1px solid hsl(var(--border-100)); }
.change-row + .change-row { margin-top: 6px; }
.change-row .kind { flex: none; }
.change-row .txt { flex: 1; font-size: var(--fs-13); color: hsl(var(--text-100) / .92); }
.change-row[data-decision="accept"] { box-shadow: inset 2px 0 0 hsl(var(--accent-brand)); }
.change-row[data-decision="reject"] .txt { color: hsl(var(--text-200) / .7); text-decoration: line-through solid hsl(var(--border-200)); }
.reason { margin: 6px 0 0; }
.issue { padding: 10px 12px; border-radius: var(--r-ctl); background: hsl(var(--bg-200)); border: 1px solid hsl(var(--border-100)); }
.issue + .issue { margin-top: 8px; }
.issue .top { display: flex; align-items: center; gap: var(--s2); margin-bottom: 4px; }
.issue .q { font-size: var(--fs-13); color: hsl(var(--text-100) / .92); }
.issue .sug { margin-top: 4px; font-size: var(--fs-13); color: hsl(var(--text-200) / .82); }
.anchor { display: inline-flex; align-items: center; gap: 4px; font-size: var(--fs-12); color: hsl(var(--info)); border-bottom: 1px dashed hsl(var(--info) / .5); }
.anchor:hover { color: hsl(var(--info)); background: hsl(var(--info) / .1); }
.impact-card { border: 1px solid hsl(var(--accent-brand) / .4); background: hsl(var(--accent-brand) / .07); border-radius: var(--r-ctl); padding: 10px 12px; font-size: var(--fs-13); }
.impact-card .big { font-family: var(--font-mono); font-weight: 600; color: hsl(var(--accent-text)); }
.dup-note { border: 1px dashed hsl(var(--border-200)); border-radius: var(--r-ctl); padding: 10px 12px; font-size: var(--fs-12); color: hsl(var(--text-200) / .78); }

/* ═══ 阅读与版本 ═══ */
.read-wrap { display: grid; grid-template-columns: minmax(0, 1fr) 232px; gap: var(--s7); align-items: start; }
.read-aside { font-size: var(--fs-13); }
.read-aside .card + .card { margin-top: var(--s3); }
.ver-row { display: flex; align-items: center; gap: var(--s3); padding: 12px 14px; border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); background: hsl(var(--bg-200)); }
.ver-row + .ver-row { margin-top: 8px; }
.ver-row[data-current="1"] { border-color: hsl(var(--accent-brand) / .5); box-shadow: inset 2px 0 0 hsl(var(--accent-brand)); }
.ver-row[data-inactive="1"] { opacity: .62; }
.ver-badge { flex: none; width: 42px; height: 28px; display: grid; place-items: center; border-radius: var(--r-key); font-family: var(--font-mono); font-weight: 700; font-size: var(--fs-13); background: hsl(var(--bg-300)); color: hsl(var(--text-100) / .88); }
.ver-row[data-current="1"] .ver-badge { background: hsl(var(--accent-brand)); color: hsl(var(--on-accent)); }
.ver-row .txt { flex: 1; min-width: 0; }
.ver-row .sum { font-size: var(--fs-13); color: hsl(var(--text-100) / .9); }
.ver-row .meta { margin-top: 2px; font-size: var(--fs-12); color: hsl(var(--text-200) / .7); }
.ver-row .acts { display: flex; gap: 6px; flex: none; }

/* ═══ 伏笔 / 债务 / 时间线 / 记忆 ═══ */
.fcard { border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); background: hsl(var(--bg-200)); padding: var(--s4); }
.fcard .top { display: flex; align-items: center; gap: var(--s2); margin-bottom: 6px; }
.fcard .code { font-family: var(--font-mono); font-size: var(--fs-12); font-weight: 700; color: hsl(var(--text-000)); }
.fcard .txt { font-size: var(--fs-14); color: hsl(var(--text-000)); }
.fcard .grid2 { margin-top: 10px; display: grid; grid-template-columns: 1fr 1fr; gap: 4px var(--s4); font-size: var(--fs-12); color: hsl(var(--text-200) / .78); }
.fcard .grid2 b { font-weight: 600; color: hsl(var(--text-100) / .9); }
.debt-row { display: grid; grid-template-columns: 1fr 168px 104px; gap: var(--s4); align-items: center; padding: 12px 14px; border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); background: hsl(var(--bg-200)); }
.debt-row + .debt-row { margin-top: 8px; }
.debt-row.warn { border-color: hsl(var(--warn) / .45); }
.track { position: relative; height: 14px; }
.track .base { position: absolute; inset: 6px 0 auto; height: 2px; background: hsl(var(--border-100)); }
.track .bar { position: absolute; height: 6px; top: 4px; border-radius: 999px; background: hsl(var(--accent-brand) / .6); }
.track .ticks { display: flex; justify-content: space-between; font-family: var(--font-mono); font-size: 9px; color: hsl(var(--text-200) / .6); margin-top: 8px; }
.tl { position: relative; padding-left: 26px; }
.tl::before { content: ""; position: absolute; left: 7px; top: 6px; bottom: 0; width: 1px; background: hsl(var(--border-100)); }
.tl-item { position: relative; padding-bottom: var(--s5); }
.tl-item:last-child { padding-bottom: 0; }
.tl-item::before { content: ""; position: absolute; left: -22px; top: 5px; width: 9px; height: 9px; border-radius: 50%; background: hsl(var(--bg-000)); border: 2px solid hsl(var(--border-200)); }
.tl-item.now::before { border-color: hsl(var(--accent-brand)); background: hsl(var(--accent-brand)); }
.tl-item .day { font-family: var(--font-mono); font-size: var(--fs-12); color: hsl(var(--text-200) / .75); }
.tl-item.now .day { color: hsl(var(--accent-text)); font-weight: 600; }
.tl-item .t { margin-top: 2px; font-size: var(--fs-14); color: hsl(var(--text-000)); }
.tl-item .d { margin-top: 3px; font-size: var(--fs-13); color: hsl(var(--text-200) / .82); }
.mem-band { position: relative; height: 6px; border-radius: 999px; background: hsl(var(--bg-300)); margin: 8px 0 6px; }
.mem-band > i { position: absolute; top: 0; height: 100%; border-radius: 999px; background: hsl(var(--accent-brand) / .65); }
.char-card { display: flex; gap: var(--s3); align-items: flex-start; text-align: left; width: 100%; padding: var(--s3); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); background: hsl(var(--bg-200)); transition: background var(--t-fast), border-color var(--t-fast); }
.char-card:hover { background: hsl(var(--bg-300)); border-color: hsl(var(--border-200)); }
.seal { flex: none; width: 34px; height: 34px; border-radius: 8px; display: grid; place-items: center; font-family: var(--font-serif); font-size: var(--fs-16); color: hsl(var(--on-accent)); background: hsl(var(--accent-brand) / .85); }
.rel-line { padding: 10px 12px; border: 1px solid hsl(var(--border-100)); border-radius: var(--r-ctl); background: hsl(var(--bg-200)); }
.rel-line + .rel-line { margin-top: 6px; }
.rel-line .pair { font-size: var(--fs-13); font-weight: 600; color: hsl(var(--text-000)); }
.rel-line .dir { display: grid; grid-template-columns: 62px 1fr; gap: 2px 8px; margin-top: 5px; font-size: var(--fs-12); color: hsl(var(--text-200) / .82); }
.rel-line .dir .k { color: hsl(var(--text-200) / .68); }

/* ═══ 线程（对话流） ═══ */
.thread-wrap { display: grid; grid-template-columns: 232px minmax(0, 1fr); height: 100%; min-height: 0; }
.thread-list { border-right: 1px solid hsl(var(--border-100)); padding: var(--s3) var(--s2); overflow-y: auto; background: hsl(var(--bg-100)); }
.convo { overflow-y: auto; padding: var(--s5) var(--s6) var(--s6); min-height: 0; }
.turn { max-width: 640px; }
.turn + .turn { margin-top: var(--s5); }
.turn.me { margin-left: auto; }
.who { display: flex; align-items: center; gap: 6px; font-size: var(--fs-12); color: hsl(var(--text-200) / .7); margin-bottom: 6px; }
.bubble { padding: 10px 13px; border-radius: var(--r-card); font-size: var(--fs-14); line-height: 1.75; background: hsl(var(--bg-200)); border: 1px solid hsl(var(--border-100)); color: hsl(var(--text-100) / .92); }
.turn.me .bubble { background: hsl(var(--accent-brand) / .13); border-color: hsl(var(--accent-brand) / .3); }
.tool-line { display: flex; align-items: center; gap: 7px; font-size: var(--fs-12); color: hsl(var(--text-200) / .78); padding: 5px 8px; border-radius: var(--r-key); background: hsl(var(--bg-100)); border: 1px dashed hsl(var(--border-100)); }
.tool-line + .tool-line { margin-top: 4px; }
.inline-card { margin-top: var(--s3); border: 1px solid hsl(var(--accent-brand) / .4); background: hsl(var(--bg-000)); border-radius: var(--r-card); padding: var(--s4); box-shadow: var(--sh-2); }
.inline-card .acts { display: flex; flex-wrap: wrap; gap: 6px; margin-top: var(--s3); }
.finding { border-left: 2px solid hsl(var(--warn)); padding-left: 10px; }

/* ═══ 进阶面 / 诊断 ═══ */
.adv-tabs { display: flex; gap: 4px; padding: var(--s2) var(--s6) 0; border-bottom: 1px solid hsl(var(--border-100)); flex-wrap: wrap; }
.adv-tab { height: 28px; padding: 0 11px; border-radius: var(--r-ctl) var(--r-ctl) 0 0; font-size: var(--fs-12); color: hsl(var(--text-200) / .8); border: 1px solid transparent; border-bottom: 0; }
.adv-tab:hover { background: hsl(var(--bg-100)); color: hsl(var(--text-000)); }
.adv-tab[aria-current="page"] { background: hsl(var(--bg-200)); border-color: hsl(var(--border-100)); color: hsl(var(--text-000)); font-weight: 600; }
.dtable { width: 100%; border-collapse: collapse; font-size: var(--fs-12); }
.dtable th { text-align: left; font-weight: 500; color: hsl(var(--text-200) / .72); padding: 7px 10px; border-bottom: 1px solid hsl(var(--border-100)); font-family: var(--font-mono); }
.dtable td { padding: 7px 10px; border-bottom: 1px solid hsl(var(--border-100)); font-family: var(--font-mono); color: hsl(var(--text-100) / .88); vertical-align: top; }
.dtable tr:hover td { background: hsl(var(--bg-100)); }
.code { font-family: var(--font-mono); font-size: var(--fs-12); line-height: 1.65; background: hsl(var(--bg-300)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-ctl); padding: 10px 12px; color: hsl(var(--text-100) / .9); overflow-x: auto; white-space: pre; }
.tree { font-family: var(--font-mono); font-size: var(--fs-12); }
.tree .n { display: flex; align-items: center; gap: 8px; padding: 5px 8px; border-radius: var(--r-key); }
.tree .n:hover { background: hsl(var(--bg-100)); }
.tree .n .l1 { padding-left: 0; } .tree .n .l2 { padding-left: 18px; } .tree .n .l3 { padding-left: 36px; }
.tree .n .st { margin-left: auto; display: flex; align-items: center; gap: 6px; }

/* ═══ 抽屉 / 弹层 / 轻提示 ═══ */
.scrim { position: absolute; inset: 0; background: hsl(30 10% 6% / .28); z-index: 40; animation: fade var(--t-base) ease-out; }
@keyframes fade { from { opacity: 0; } to { opacity: 1; } }
.drawer {
  position: absolute; top: 0; right: 0; bottom: 0; width: 448px; z-index: 45;
  background: hsl(var(--bg-100)); border-left: 1px solid hsl(var(--border-100));
  box-shadow: var(--sh-3); display: flex; flex-direction: column;
  animation: slidein var(--t-base) cubic-bezier(.2, 0, 0, 1);
}
@keyframes slidein { from { transform: translateX(24px); opacity: .4; } to { transform: none; opacity: 1; } }
.drawer-head { display: flex; align-items: flex-start; gap: var(--s3); padding: var(--s4) var(--s5); border-bottom: 1px solid hsl(var(--border-100)); }
.drawer-body { flex: 1; overflow-y: auto; padding: var(--s4) var(--s5) var(--s6); }
.drawer-foot { border-top: 1px solid hsl(var(--border-100)); padding: var(--s3) var(--s5); display: flex; gap: var(--s2); }
.modal-layer { position: absolute; inset: 0; z-index: 50; display: grid; place-items: center; background: hsl(30 10% 6% / .3); animation: fade var(--t-base) ease-out; }
.modal { width: 520px; max-width: calc(100% - 48px); max-height: calc(100% - 80px); overflow-y: auto; background: hsl(var(--bg-100)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); box-shadow: var(--sh-3); padding: var(--s5); }
.modal.wide { width: 640px; }
.modal h3 { font-size: var(--fs-16); color: hsl(var(--text-000)); }
.modal-foot { display: flex; justify-content: flex-end; gap: var(--s2); margin-top: var(--s5); }
.toast {
  position: absolute; z-index: 60; left: 50%; bottom: 108px; transform: translateX(-50%);
  display: flex; align-items: center; gap: 8px;
  padding: 9px 16px; border-radius: 999px;
  background: hsl(var(--text-000)); color: hsl(var(--bg-100));
  font-size: var(--fs-13); box-shadow: var(--sh-3);
  animation: toastin var(--t-base) cubic-bezier(.2, 0, 0, 1);
}
@keyframes toastin { from { opacity: 0; transform: translate(-50%, 8px); } to { opacity: 1; transform: translate(-50%, 0); } }
.settings-pop { position: absolute; z-index: 55; top: 44px; right: 12px; width: 300px; background: hsl(var(--bg-100)); border: 1px solid hsl(var(--border-100)); border-radius: var(--r-card); box-shadow: var(--sh-3); padding: var(--s4); }

/* ═══ 边界态 ═══ */
.hero-card { max-width: 560px; margin: 8% auto 0; text-align: center; }
.hero-card h2 { font-size: var(--fs-20); margin-bottom: var(--s3); }
.hero-card ol { text-align: left; margin: var(--s4) 0 var(--s5); padding-left: 0; list-style: none; display: grid; gap: var(--s3); }
.hero-card li { display: grid; grid-template-columns: 22px 1fr; gap: var(--s3); font-size: var(--fs-13); color: hsl(var(--text-200) / .85); }
.hero-card li .i { font-family: var(--font-mono); color: hsl(var(--accent-text)); }
.err-card { max-width: 520px; margin: 6% auto 0; border: 1px solid hsl(var(--err) / .4); background: hsl(var(--err) / .06); border-radius: var(--r-card); padding: var(--s5); }
.err-card h3 { font-size: var(--fs-16); color: hsl(var(--text-000)); }
.warn-strip { display: flex; align-items: center; gap: var(--s2); padding: 9px 12px; border-radius: var(--r-ctl); font-size: var(--fs-13); border: 1px solid hsl(var(--warn) / .4); background: hsl(var(--warn) / .08); color: hsl(var(--text-100) / .9); }
.warn-strip.err { border-color: hsl(var(--err) / .42); background: hsl(var(--err) / .07); }

/* ═══ 窄窗 1280 ═══ */
.app[data-narrow="1"] { grid-template-columns: 58px minmax(0, 1fr) 0; }
.app[data-narrow="1"] .rail-scroll { padding: var(--s3) 6px; }
.app[data-narrow="1"] .rail, .app[data-narrow="1"] .rail-scroll { overflow: visible; }
.app[data-narrow="1"] .rail .item { justify-content: center; padding: 7px 0; }
.app[data-narrow="1"] .rail .lbl, .app[data-narrow="1"] .rail .grp-head, .app[data-narrow="1"] .rail .tail,
.app[data-narrow="1"] .rail .vol-head, .app[data-narrow="1"] .rail-foot .lbl { display: none; }
.app[data-narrow="1"] .rail .ico { width: 17px; height: 17px; }
.app[data-narrow="1"] .side { display: none; }
.app[data-narrow="1"] .side.open { display: flex; position: absolute; top: 44px; bottom: 88px; right: 0; width: 296px; z-index: 35; box-shadow: var(--sh-3); }
.tipwrap { position: relative; }
.tipwrap .tip {
  position: absolute; left: calc(100% + 8px); top: 50%; transform: translateY(-50%);
  background: hsl(var(--text-000)); color: hsl(var(--bg-100)); font-size: var(--fs-12);
  padding: 4px 8px; border-radius: var(--r-key); white-space: nowrap; opacity: 0; pointer-events: none;
  transition: opacity var(--t-fast); z-index: 30;
}
.tipwrap:hover .tip, .tipwrap:focus-within .tip { opacity: 1; }

`
